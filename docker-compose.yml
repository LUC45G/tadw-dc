version: '3.3'
services:
    mongo:
        build:
            context: ./mongodb
            dockerfile: Dockerfile
        ports:
            - '27017:27017'
        volumes:
            - mongodb-data:/data/db
volumes:
    mongodb-data:
